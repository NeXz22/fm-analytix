import { HeaderLabelPipe } from './header-label.pipe';

describe('HeaderLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new HeaderLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
