import { TransformDataTypePipe } from './transform-data-type.pipe';

describe('TransformDataTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformDataTypePipe();
    expect(pipe).toBeTruthy();
  });
});
