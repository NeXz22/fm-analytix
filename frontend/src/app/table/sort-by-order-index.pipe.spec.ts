import { SortByOrderIndexPipe } from './sort-by-order-index.pipe';

describe('SortByOrderIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByOrderIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
